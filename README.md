# 암호화폐 학습 및 돈의 흐름 대시보드

초보자가 암호화폐 개념, 시장 데이터, 돈의 흐름을 함께 공부하는 로컬/모바일 공유용 Next.js 대시보드입니다.

## 실행

```powershell
npm install
npm run dev
```

PC에서 `http://localhost:3000`, 같은 네트워크의 휴대폰에서는 `http://PC-IP:3000`으로 접속합니다.

## 구현 순서

1. 홈 화면과 공통 레이아웃
2. 학습 콘텐츠와 용어장
3. 서버 API 기반 실제 데이터 연결
4. 규칙 기반 돈의 흐름 엔진
